project Sort extends "../rts/akernel_program.gpr" is
   for Source_Dirs use (".");
   for Exec_Dir use "../../bin/userspace";
   for Object_Dir use "../../obj/userspace/sort";
   for Main use ("sort.adb");

   package Builder is
      for Executable ("sort.adb") use "sort.elf";
   end Builder;
end Sort;
