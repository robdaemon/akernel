name = "akernel_virtio_gpu"
description = "akernel virtio-gpu user-space driver"
version = "0.1.0-dev"

authors = ["Robert Roland"]
maintainers = ["Robert Roland <rob@retronauts.org>"]
licenses = "MIT OR Apache-2.0 WITH LLVM-exception"
tags = []

project-files = ["virtio_gpu.gpr"]
executables = ["virtio_input.elf"]

[[depends-on]]
gnat_riscv64_elf = "*"
akernel_virtio = "*"

[[pins]]
akernel_virtio = { path = "../virtio" }
