project Assign extends "../rts/akernel_program.gpr" is
   for Source_Dirs use (".");
   for Exec_Dir use "../../bin/userspace";
   for Object_Dir use "../../obj/userspace/assign";
   for Main use ("assign.adb");

   package Builder is
      for Executable ("assign.adb") use "assign.elf";
   end Builder;
end Assign;
