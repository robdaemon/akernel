project Wait extends "../rts/akernel_program.gpr" is
   for Source_Dirs use (".");
   for Exec_Dir use "../../bin/userspace";
   for Object_Dir use "../../obj/userspace/wait";
   for Main use ("wait.adb");

   package Builder is
      for Executable ("wait.adb") use "wait.elf";
   end Builder;
end Wait;
