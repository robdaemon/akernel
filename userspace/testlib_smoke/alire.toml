name = "akernel_testlib_smoke"
description = "akernel Testlib non-interactive smoke test (milestone 58)"
version = "0.1.0-dev"

authors = ["Robert Roland"]
maintainers = ["Robert Roland <rob@retronauts.org>"]
licenses = "MIT OR Apache-2.0 WITH LLVM-exception"
tags = []

project-files = ["testlib_smoke.gpr"]
executables = ["testlib_smoke.elf"]

[[depends-on]]
gnat_riscv64_elf = "*"
