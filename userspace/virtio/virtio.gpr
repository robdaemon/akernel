library project Virtio is
   for Source_Dirs use (".");
   for Object_Dir use "../../obj/userspace/virtio";
   for Create_Missing_Dirs use "True";
   for Library_Dir use "../../lib/userspace/virtio";
   for Library_Name use "akernel_virtio";
   for Library_Kind use "static";
   for Languages use ("Ada");
   for Target use "riscv64-elf";
   for Runtime ("Ada") use "light-rv64imafdc";
end Virtio;
