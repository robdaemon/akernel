name = "akernel_virtio_rng"
description = "akernel virtio-rng user-space driver"
version = "0.1.0-dev"

authors = ["Robert Roland"]
maintainers = ["Robert Roland <rob@retronauts.org>"]
licenses = "MIT OR Apache-2.0 WITH LLVM-exception"
tags = []

project-files = ["virtio_rng.gpr"]
executables = ["virtio_rng.elf"]

[[depends-on]]
gnat_riscv64_elf = "15.3.1"
akernel_virtio = "*"

[[pins]]
akernel_virtio = { path = "../virtio" }
