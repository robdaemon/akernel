with Tdemo_App;

--  Tdemo (milestone 56): proves the Trinket widget tree — nested
--  groups, inset field, labels, clickable buttons — on the
--  uniform program ABI (handle 3 = Bureau window service).
--  Button clicks report through the debug console; the close
--  gadget ends the event loop and the window is destroyed.
procedure Tdemo is
begin
   Tdemo_App.Main;
end Tdemo;
