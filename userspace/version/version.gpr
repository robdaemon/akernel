project Version extends "../rts/akernel_program.gpr" is
   for Source_Dirs use (".");
   for Exec_Dir use "../../bin/userspace";
   for Object_Dir use "../../obj/userspace/version";
   for Main use ("version.adb");

   package Builder is
      for Executable ("version.adb") use "version.elf";
   end Builder;
end Version;
